import React from 'react'

function Post() {
    return (
        <div>
            {/* Header -> avatar + name + ... */}
            {/* Picture + tag*/}
            {/* Action bar -> like + comment + share + bookmark */}
            {/* Who liked */}
            {/* Comment -> name + comment + like */}
            {/* Add a comment  */}
        </div>
    )
}

export default Post
